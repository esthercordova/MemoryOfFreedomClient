<h1>Memory of Freedom</h1>

<h3> Which questions do you want to study? </h1>

{{#if nNew }}
  <button id="newBucket" class="btn btn-grey">New</button>
{{/if}}
{{#if nEasy }}
  <button id="easyBucket" class="btn btn-blue">Easy</button>
{{/if}}
{{#if nHard }}
  <button id="hardBucket" class="btn btn-red">Hard</button>
{{/if}}
